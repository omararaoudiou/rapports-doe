Cette version prépare une compatibilité iOS via WKWebView.
À brancher :
- NativeBridge.swift
- gestion OCR iOS
- export fichier iOS
- éventuel scan réseau natif si disponible
Le cœur métier reste le même que la version Android / PWA.
